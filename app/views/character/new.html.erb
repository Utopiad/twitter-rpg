<%= form_for @character, url: {action: "create"} do |f| -%>
  <%= f.text_field :name %> <%= f.label :name %> <br>
  <%= f.select :character_type_id, @world_characters_type %>
  <p><%= f.submit "Create" -%></p>
<% end %>
